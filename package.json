{
  "name": "rota-do-caso",
  "version": "0.1.0",
  "private": true,
  "description": "Portal de organização de casos e documentos para escritórios jurídicos.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/server-helpers.test.js",
    "test:integration": "node --env-file=.env.test --test test/integration.test.js",
    "test:integration:db": "docker compose -f docker-compose.test.yml up -d",
    "migrate": "node migrate.js",
    "seed": "node seed.js",
    "seed:demo": "node seed.js demo"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "mysql2": "^3.12.0"
  }
}
