# /etc/nginx/sites-available/rotadocaso.com.br
#
# Este virtual host recebe somente o dominio Rota do Caso. Ele NAO deve ser
# marcado como "default_server", pois o VPS pode servir outros dominios.
server {
    listen 80;
    listen [::]:80;
    server_name rotadocaso.com.br www.rotadocaso.com.br;

    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Connection "";
        proxy_read_timeout 60s;
    }
}
