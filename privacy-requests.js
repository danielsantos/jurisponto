document.querySelector('#privacy-request').addEventListener('submit',async(e)=>{e.preventDefault();const form=e.currentTarget;const result=document.querySelector('#result');const response=await fetch('/api/privacy/requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const payload=await response.json();if(!response.ok){alert(payload.error?.message||'Não foi possível enviar a solicitação.');return}form.reset();result.hidden=false;result.textContent=payload.data.message});
